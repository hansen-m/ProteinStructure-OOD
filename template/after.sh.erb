#sleep 5
# Wait for the ttrd  Server to start
#echo "Waiting ttyd Server to open port ${port}..."
#if wait_until_port_used "${host}:${port}" 60; then
#  echo "Discovered ttyd Server listening on port ${port}!"
#else
#  echo "Timed out waiting for ttyd Server to open port ${port}!"
#  clean_up 1
#fi
#sleep 2
